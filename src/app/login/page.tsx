import LoginComponent from "@/components/LoginComponent";

export default function Login(){
    return (
        <main>
            <div>Login</div>
            <LoginComponent/>
        </main>
    );
}